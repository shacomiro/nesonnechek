spring:
  config:
    import:
      - application-domain-rds.yaml
  profiles:
    group:
      dev:
        - domain-rds-dev
      local:
        - domain-rds-local
    active: ${profile}
  batch:
    job:
      names: ${job.name:NONE}
    jdbc:
      initialize-schema: always
  main:
    web-application-type: none
